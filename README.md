## Docker Settings

docker run -it -p 3001:3001 -v $PWD:$PWD rmerces/markitdown

## Gemini CLI Settings

```json
  "mcpServers": {
    "markitdown": {
      "httpUrl": "http://localhost:3001/mcp",
    }
  }

```
